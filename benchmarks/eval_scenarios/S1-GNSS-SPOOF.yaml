# 오프라인 KPI 평가셋 + 골든 픽스처(issue #83) — 재구성본
# expected_* 필드는 detection golden fixture → response runbook trace 검증용
# (fixture -> tactic -> CACAO playbook 선택 -> asset_id -> mission_continuity).
# 값 출처: dah-sentinel-content AnalyticsRules/S1_GNSS_Spoofing.json(tactics/techniques),
# core/policy/cacao-playbooks.yaml(playbook--uav-impact-0001, tactic=Impact 매칭),
# core/policy/degradation-matrix.yaml(GNSS=SUSTAINED).
fixture_id: FX-S1-GNSS-SPOOF
scenario_id: S1-GNSS-SPOOFING
title: GPS/GNSS 스푸핑 (EKF Variance Anomaly)
asset_id: GNSS
target_asset:
  id: GNSS
  tier: T1-Critical
mission_context:
  phase: on-station
severity_baseline: h
telemetry:
  signals:
    - "GNSS-INS 잔차 급증 (PosHorizVariance 정상 대비 7배)"
    - "C/N0 비정상 상승"
    - "위성수 급감"
expected_detection:
  sentinel_rule: S1_GNSS_Spoofing.json
mitre:
  attack_ics: ["T0835", "T0832", "T0831"]
expected_tactic: Collection
expected_techniques: ["T1565", "T1565.001", "T1565.002"]
expected_cacao_playbook_id: playbook--uav-coll-0001
expected_runbook_id: RB-S1-GNSS-SPOOFING
expected_resilience_level: SUSTAINED
expected_fallback_contains: "INS/관성항법"
expected_report_evidence: "위성항법 정확도"
defense_playbook:
  id: PB-NAV-RTB-01
  actions:
    - "GNSS 신뢰도 강등, INS/관성항법으로 페일오버"
    - "마지막 신뢰 좌표 기준 항법 재계산"
    - "운용자 경보 + 자동 RTB 후보 경로 제시"
  failover: "INS 단독 항법으로 즉시 전환 후 RTB"
  onboard_defense:
    - "온보드 센서퓨전(GPS-IMU 교차검증)으로 통신 없이 GPS 자율 강등"

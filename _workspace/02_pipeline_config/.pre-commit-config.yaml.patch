--- a/.pre-commit-config.yaml
+++ b/.pre-commit-config.yaml
@@ -36,18 +36,32 @@

   # ── ruff — 린터 + isort ─────────────────
   - repo: https://github.com/astral-sh/ruff-pre-commit
-    rev: v0.4.9
+    # CI와 동일 버전 (pyproject dev extras: ruff>=0.5.0,<0.6)
+    rev: v0.5.7
     hooks:
       - id: ruff
-        args: ["--fix"]                # 자동 수정 가능한 건 자동 수정
+        # 자동 수정 발생 시에도 실패로 처리 — 로컬에서 CI 정합 사전 차단
+        args: ["--fix", "--exit-non-zero-on-fix", "--output-format=github"]
       - id: ruff-format

   # ── mypy — 타입 검사 ────────────────────
   - repo: https://github.com/pre-commit/mirrors-mypy
     rev: v1.10.0
     hooks:
       - id: mypy
+        # CI의 mypy 검사와 동일 결과를 내기 위해 런타임 의존성 동기화
         additional_dependencies:
           - pydantic>=2.7.0
           - pydantic-settings>=2.3.0
           - types-httpx
+          - httpx>=0.27.0
+          - langchain-core>=0.3.0
+          - langgraph>=0.2.0
+          - jsonschema>=4.0.0
+          - compliance-trestle>=3.0.0
+
+  # ── interrogate — 독스트링 커버리지 ─────
+  - repo: https://github.com/econchick/interrogate
+    rev: 1.7.0
+    hooks:
+      - id: interrogate
+        args: ["-c", "pyproject.toml", "."]
+        pass_filenames: false
